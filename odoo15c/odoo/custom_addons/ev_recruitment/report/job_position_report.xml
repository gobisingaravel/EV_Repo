<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Job Position Report -->
        <record id="action_report_job_position" model="ir.actions.report">
            <field name="name">Job Position</field>
            <field name="model">hr.job</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name">ev_recruitment.report_jobposition</field>
            <field name="report_file">ev_recruitment.report_jobposition</field>
            <field name="print_report_name">('Job Position - %s' % (object.name))</field>
            <field name="binding_model_id" ref="model_hr_job"/>
            <field name="binding_type">report</field>
        </record>

    </data>
</odoo>
